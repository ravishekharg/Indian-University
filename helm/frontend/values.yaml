replicaCount: 3

image:
  repository: ACCOUNT_ID.dkr.ecr.ap-south-2.amazonaws.com/university-frontend
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

backend:
  apiUrl: http://backend-service:5000

resources:

  requests:
    cpu: 100m
    memory: 128Mi

  limits:
    cpu: 300m
    memory: 512Mi

ingress:
  enabled: true
  host: gurram-university.com