apiVersion: v1
kind: ConfigMap

metadata:
  name: university-grafana-dashboards

  labels:
    grafana_dashboard: "1"

data:

  business.json: |
{{ .Files.Get "dashboards/business.json" | indent 4 }}

  application.json: |
{{ .Files.Get "dashboards/application.json" | indent 4 }}

  database.json: |
{{ .Files.Get "dashboards/database.json" | indent 4 }}