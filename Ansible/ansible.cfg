[defaults]

inventory = inventories/dev/hosts.ini
roles_path = ./roles
host_key_checking = False
timeout = 60
forks = 20
remote_user = ec2-user
private_key_file = ~/.ssh/gurram.pem
become = True
